name: V4 Run8 Hardening
on:
  push:
    branches: [v4]
    paths: ['.github/workflows/v4-run8-hardening.yml']
permissions:
  contents: write
jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v4
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply Web cookie/CSRF and Google Photos tenant hardening
        run: |
          python3 - <<'PY'
          from pathlib import Path

          bridge = Path('desktop/src/bridge.ts')
          s = bridge.read_text()
          s = s.replace("  if (!config?.baseUrl || (!config.accessToken&&!config.refreshToken)) return undefined;", "  if (!config?.baseUrl) return undefined;")
          s = s.replace("  let accessToken=config.accessToken||'';\n  let refreshToken=config.refreshToken||'';\n  let refreshPromise:Promise<boolean>|null=null;", "  let accessToken=config.accessToken||'';\n  let bootstrapRefreshToken=config.refreshToken||'';\n  let csrfToken='';\n  let refreshPromise:Promise<boolean>|null=null;\n  let bootstrapPromise:Promise<boolean>|null=null;\n\n  function cookie(name:string){\n    if(typeof document==='undefined')return '';\n    const prefix=`${encodeURIComponent(name)}=`;\n    return document.cookie.split(';').map(v=>v.trim()).find(v=>v.startsWith(prefix))?.slice(prefix.length)||'';\n  }\n  csrfToken=decodeURIComponent(cookie('photox_csrf')||'');")
          old = """  function persistTokens(){
    if(typeof sessionStorage==='undefined')return;
    if(accessToken)sessionStorage.setItem('photox.web.access',accessToken);else sessionStorage.removeItem('photox.web.access');
    if(refreshToken)sessionStorage.setItem('photox.web.refresh',refreshToken);else sessionStorage.removeItem('photox.web.refresh');
  }

  async function refreshAccess(){
    if(!refreshToken)return false;
    if(refreshPromise)return refreshPromise;
    refreshPromise=(async()=>{
      try{
        const response=await fetch(`${baseUrl}/api/web/v1/auth/refresh`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({refreshToken})});
        if(!response.ok)throw new Error('WEB_REFRESH_REJECTED');
        const next=await response.json() as {accessToken?:string};
        if(!next.accessToken)throw new Error('WEB_REFRESH_TOKEN_MISSING');
        accessToken=next.accessToken;persistTokens();return true;
      }catch{accessToken='';refreshToken='';persistTokens();return false;}
      finally{refreshPromise=null;}
    })();
    return refreshPromise;
  }
"""
          new = """  function persistAccess(){
    if(typeof sessionStorage==='undefined')return;
    if(accessToken)sessionStorage.setItem('photox.web.access',accessToken);else sessionStorage.removeItem('photox.web.access');
  }

  async function bootstrapSession(){
    if(!bootstrapRefreshToken)return false;
    if(bootstrapPromise)return bootstrapPromise;
    bootstrapPromise=(async()=>{
      try{
        const token=bootstrapRefreshToken;bootstrapRefreshToken='';
        const response=await fetch(`${baseUrl}/api/web/v1/auth/bootstrap`,{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({refreshToken:token})});
        if(!response.ok)throw new Error('WEB_BOOTSTRAP_REJECTED');
        const next=await response.json() as {accessToken?:string;csrfToken?:string};
        if(!next.accessToken)throw new Error('WEB_BOOTSTRAP_ACCESS_MISSING');
        accessToken=next.accessToken;csrfToken=next.csrfToken||decodeURIComponent(cookie('photox_csrf')||'');persistAccess();return true;
      }catch{accessToken='';persistAccess();return false;}
      finally{bootstrapPromise=null;}
    })();
    return bootstrapPromise;
  }

  async function refreshAccess(){
    if(refreshPromise)return refreshPromise;
    refreshPromise=(async()=>{
      try{
        if(!csrfToken)csrfToken=decodeURIComponent(cookie('photox_csrf')||'');
        const response=await fetch(`${baseUrl}/api/web/v1/auth/refresh`,{method:'POST',credentials:'include',headers:csrfToken?{'x-csrf-token':csrfToken}:{}});
        if(!response.ok)throw new Error('WEB_REFRESH_REJECTED');
        const next=await response.json() as {accessToken?:string;csrfToken?:string};
        if(!next.accessToken)throw new Error('WEB_REFRESH_TOKEN_MISSING');
        accessToken=next.accessToken;csrfToken=next.csrfToken||csrfToken||decodeURIComponent(cookie('photox_csrf')||'');persistAccess();return true;
      }catch{accessToken='';persistAccess();return false;}
      finally{refreshPromise=null;}
    })();
    return refreshPromise;
  }
"""
          if old not in s: raise SystemExit('bridge auth block not found')
          s = s.replace(old,new)
          old = """  async function json<T>(path:string,init:RequestInit={},retry=true):Promise<T>{
    if(!accessToken&&refreshToken)await refreshAccess();
    const response=await fetch(`${baseUrl}${path}`,{...init,headers:{...(accessToken?{authorization:`Bearer ${accessToken}`}:{ }),...(init.body?{'content-type':'application/json'}:{}),...(init.headers||{})}});
    if(response.status===401&&retry&&refreshToken&&await refreshAccess())return json<T>(path,init,false);
"""
          new = """  async function json<T>(path:string,init:RequestInit={},retry=true):Promise<T>{
    if(!accessToken&&bootstrapRefreshToken)await bootstrapSession();
    if(!accessToken)await refreshAccess();
    const method=String(init.method||'GET').toUpperCase();
    if(!csrfToken)csrfToken=decodeURIComponent(cookie('photox_csrf')||'');
    const response=await fetch(`${baseUrl}${path}`,{...init,credentials:'include',headers:{...(accessToken?{authorization:`Bearer ${accessToken}`}:{ }),...(method!=='GET'&&method!=='HEAD'&&csrfToken?{'x-csrf-token':csrfToken}:{}),...(init.body?{'content-type':'application/json'}:{}),...(init.headers||{})}});
    if(response.status===401&&retry&&await refreshAccess())return json<T>(path,init,false);
"""
          if old not in s: raise SystemExit('bridge json block not found')
          s=s.replace(old,new)
          s=s.replace("      if(!accessToken&&refreshToken)await refreshAccess();\n      if(!accessToken)return;", "      if(!accessToken&&bootstrapRefreshToken)await bootstrapSession();\n      if(!accessToken)await refreshAccess();\n      if(!accessToken)return;")
          bridge.write_text(s)

          web = Path('desktop/electron/webEdgeServer.ts')
          s=web.read_text()
          s=s.replace("const MEDIA_TTL_SECONDS=10*60;", "const MEDIA_TTL_SECONDS=10*60;\nconst WEB_REFRESH_COOKIE='photox_refresh';\nconst WEB_CSRF_COOKIE='photox_csrf';\nconst WEB_REFRESH_MAX_AGE=30*24*60*60;")
          marker = """  private requireRole(principal:WebPrincipal,role:WebRole){
    const actual=principal.workspaceRole??'viewer';
    return ROLE_RANK[actual]>=ROLE_RANK[role];
  }

"""
          insert = marker + """  private cookies(req:IncomingMessage){
    const result:Record<string,string>={};
    for(const entry of String(req.headers.cookie||'').split(';')){const at=entry.indexOf('=');if(at<=0)continue;const key=decodeURIComponent(entry.slice(0,at).trim());const value=decodeURIComponent(entry.slice(at+1).trim());result[key]=value;}
    return result;
  }

  private secureCookie(req:IncomingMessage){
    return this.config.publicBaseUrl?.startsWith('https://')||String(req.headers['x-forwarded-proto']||'').split(',')[0]?.trim()==='https';
  }

  private sessionCookies(req:IncomingMessage,refreshToken:string,csrfToken:string){
    const secure=this.secureCookie(req)?'; Secure':'';
    return [
      `${WEB_REFRESH_COOKIE}=${encodeURIComponent(refreshToken)}; Path=/api/web/v1/auth; HttpOnly; SameSite=Strict; Max-Age=${WEB_REFRESH_MAX_AGE}${secure}`,
      `${WEB_CSRF_COOKIE}=${encodeURIComponent(csrfToken)}; Path=/api/web/v1; SameSite=Strict; Max-Age=${WEB_REFRESH_MAX_AGE}${secure}`,
    ];
  }

  private clearSessionCookies(req:IncomingMessage){
    const secure=this.secureCookie(req)?'; Secure':'';
    return [
      `${WEB_REFRESH_COOKIE}=; Path=/api/web/v1/auth; HttpOnly; SameSite=Strict; Max-Age=0${secure}`,
      `${WEB_CSRF_COOKIE}=; Path=/api/web/v1; SameSite=Strict; Max-Age=0${secure}`,
    ];
  }

  private csrfValid(req:IncomingMessage){
    const cookies=this.cookies(req);const cookieToken=cookies[WEB_CSRF_COOKIE]||'';const header=String(req.headers['x-csrf-token']||'');
    if(!cookieToken||!header||cookieToken.length!==header.length)return false;
    return crypto.timingSafeEqual(Buffer.from(cookieToken),Buffer.from(header));
  }

"""
          if marker not in s: raise SystemExit('web role marker not found')
          s=s.replace(marker,insert)
          s=s.replace("res.setHeader('access-control-allow-headers','authorization,content-type,range');", "res.setHeader('access-control-allow-headers','authorization,content-type,range,x-csrf-token');\n    res.setHeader('access-control-allow-credentials','true');")
          old = """    if(url.pathname==='/api/web/v1/auth/refresh'&&req.method==='POST'){
      try{const b=await this.body(req);this.json(res,200,await this.handlers.refreshSession(String(b.refreshToken||'')));}catch(error){this.json(res,401,{error:error instanceof Error?error.message:String(error)});}return;
    }
"""
          new = """    if(url.pathname==='/api/web/v1/auth/bootstrap'&&req.method==='POST'){
      try{
        const b=await this.body(req);const refreshToken=String(b.refreshToken||'');if(!refreshToken)throw new Error('REFRESH_TOKEN_REQUIRED');
        const next=await this.handlers.refreshSession(refreshToken);const csrfToken=crypto.randomBytes(24).toString('base64url');
        res.setHeader('set-cookie',this.sessionCookies(req,refreshToken,csrfToken));this.json(res,200,{...next,csrfToken});
      }catch(error){this.json(res,401,{error:error instanceof Error?error.message:String(error)});}return;
    }
    if(url.pathname==='/api/web/v1/auth/refresh'&&req.method==='POST'){
      try{
        if(!this.csrfValid(req))throw new Error('CSRF_REQUIRED');
        const refreshToken=this.cookies(req)[WEB_REFRESH_COOKIE]||'';if(!refreshToken)throw new Error('REFRESH_TOKEN_REQUIRED');
        const next=await this.handlers.refreshSession(refreshToken);const csrfToken=this.cookies(req)[WEB_CSRF_COOKIE]||crypto.randomBytes(24).toString('base64url');
        res.setHeader('set-cookie',this.sessionCookies(req,refreshToken,csrfToken));this.json(res,200,{...next,csrfToken});
      }catch(error){res.setHeader('set-cookie',this.clearSessionCookies(req));this.json(res,401,{error:error instanceof Error?error.message:String(error)});}return;
    }
"""
          if old not in s: raise SystemExit('web refresh block not found')
          s=s.replace(old,new)
          old = """    if(m==='POST'&&p==='/api/web/v1/auth/revoke'){
      const b=await this.body(req);if(principal.sessionId&&String(b.sessionId||'')!==principal.sessionId&&!this.requireRole(principal,'admin')){this.json(res,403,{error:'ROLE_FORBIDDEN'});return;}
      await this.handlers.revokeSession(String(b.sessionId||principal.sessionId||''));res.writeHead(204);res.end();return;
    }
"""
          new = """    if(m!=='GET'&&m!=='HEAD'&&!this.csrfValid(req)){this.json(res,403,{error:'CSRF_REQUIRED'});return;}
    if(m==='POST'&&p==='/api/web/v1/auth/revoke'){
      const b=await this.body(req);if(principal.sessionId&&String(b.sessionId||'')!==principal.sessionId&&!this.requireRole(principal,'admin')){this.json(res,403,{error:'ROLE_FORBIDDEN'});return;}
      await this.handlers.revokeSession(String(b.sessionId||principal.sessionId||''));res.setHeader('set-cookie',this.clearSessionCookies(req));res.writeHead(204);res.end();return;
    }
"""
          if old not in s: raise SystemExit('web revoke block not found')
          s=s.replace(old,new)
          old = """        const bootstrap=`<script>window.__PHOTOSYNC_WEB_CONFIG__={baseUrl:location.origin,accessToken:(sessionStorage.getItem('photox.web.access')||new URLSearchParams(location.hash.slice(1)).get('access_token')||''),refreshToken:(sessionStorage.getItem('photox.web.refresh')||new URLSearchParams(location.hash.slice(1)).get('refresh_token')||'')};if(window.__PHOTOSYNC_WEB_CONFIG__.accessToken)sessionStorage.setItem('photox.web.access',window.__PHOTOSYNC_WEB_CONFIG__.accessToken);if(window.__PHOTOSYNC_WEB_CONFIG__.refreshToken)sessionStorage.setItem('photox.web.refresh',window.__PHOTOSYNC_WEB_CONFIG__.refreshToken);if(location.hash)history.replaceState(null,'',location.pathname+location.search);</script>`;
"""
          new = """        const bootstrap=`<script>const p=new URLSearchParams(location.hash.slice(1));window.__PHOTOSYNC_WEB_CONFIG__={baseUrl:location.origin,accessToken:(sessionStorage.getItem('photox.web.access')||p.get('access_token')||''),refreshToken:(p.get('refresh_token')||'')};if(window.__PHOTOSYNC_WEB_CONFIG__.accessToken)sessionStorage.setItem('photox.web.access',window.__PHOTOSYNC_WEB_CONFIG__.accessToken);sessionStorage.removeItem('photox.web.refresh');if(location.hash)history.replaceState(null,'',location.pathname+location.search);</script>`;
"""
          if old not in s: raise SystemExit('web bootstrap html not found')
          s=s.replace(old,new)
          web.write_text(s)

          gp=Path('desktop/electron/googlePhotosMigration.ts')
          s=gp.read_text()
          s=s.replace("type SavedGooglePhotosAccount = { id: string; email: string; tokens: Credentials; capabilities: GooglePhotosCapability[]; updatedAt: string };", "type SavedGooglePhotosAccount = { id: string; workspaceId: string; email: string; tokens: Credentials; capabilities: GooglePhotosCapability[]; updatedAt: string };")
          s=s.replace("function accountFilename(id: string) { return `${id}.json`; }", "function accountFilename(workspaceId: string, id: string) { const scope=crypto.createHash('sha256').update(workspaceId).digest('hex').slice(0,16); return `${scope}--${id}.json`; }")
          s=s.replace("const saved: SavedGooglePhotosAccount = { id, email, tokens: mergedTokens, capabilities, updatedAt: new Date().toISOString() };\n          await fs.writeFile(path.join(this.options.accountsDir, accountFilename(id)),", "const saved: SavedGooglePhotosAccount = { id, workspaceId: this.options.workspaceId, email, tokens: mergedTokens, capabilities, updatedAt: new Date().toISOString() };\n          await fs.writeFile(path.join(this.options.accountsDir, accountFilename(this.options.workspaceId, id)),")
          s=s.replace("  async removeAccount(accountId: string) { await fs.unlink(path.join(this.options.accountsDir, accountFilename(accountId))).catch", "  async removeAccount(accountId: string) { await this.requireAccount(accountId, (await this.requireAnyAccount(accountId)).capabilities[0]||'picker'); await fs.unlink(path.join(this.options.accountsDir, accountFilename(this.options.workspaceId, accountId))).catch")
          old = """  private async savedAccounts(): Promise<SavedGooglePhotosAccount[]> {
    await fs.mkdir(this.options.accountsDir, { recursive: true }); const result: SavedGooglePhotosAccount[] = [];
    for (const file of (await fs.readdir(this.options.accountsDir)).filter(file => file.endsWith('.json'))) try { const parsed = JSON.parse(await fs.readFile(path.join(this.options.accountsDir, file), 'utf8')) as SavedGooglePhotosAccount; if (parsed.id && parsed.email && parsed.tokens) result.push(parsed); } catch {}
    return result;
  }
  private async requireAccount(id: string, capability: GooglePhotosCapability) { const account = (await this.savedAccounts()).find(account => account.id === id); if (!account) throw new Error('GOOGLE_PHOTOS_ACCOUNT_NOT_FOUND'); if (!account.capabilities.includes(capability)) throw new Error(`GOOGLE_PHOTOS_CAPABILITY_REQUIRED:${capability}`); return account; }
"""
          new = """  private async savedAccounts(): Promise<SavedGooglePhotosAccount[]> {
    await fs.mkdir(this.options.accountsDir, { recursive: true }); const result: SavedGooglePhotosAccount[] = [];
    for (const file of (await fs.readdir(this.options.accountsDir)).filter(file => file.endsWith('.json'))) try {
      const filePath=path.join(this.options.accountsDir,file);const parsed=JSON.parse(await fs.readFile(filePath,'utf8')) as Partial<SavedGooglePhotosAccount>;
      if(!parsed.id||!parsed.email||!parsed.tokens)continue;
      if(parsed.workspaceId&&parsed.workspaceId!==this.options.workspaceId)continue;
      const scoped:SavedGooglePhotosAccount={id:parsed.id,workspaceId:this.options.workspaceId,email:parsed.email,tokens:parsed.tokens,capabilities:parsed.capabilities??[],updatedAt:parsed.updatedAt??new Date().toISOString()};
      if(!parsed.workspaceId){const target=path.join(this.options.accountsDir,accountFilename(this.options.workspaceId,scoped.id));await fs.writeFile(target,JSON.stringify(scoped,null,2),{encoding:'utf8',mode:0o600});if(target!==filePath)await fs.unlink(filePath).catch(()=>undefined);}
      result.push(scoped);
    } catch {}
    return result;
  }
  private async requireAnyAccount(id:string){const account=(await this.savedAccounts()).find(account=>account.id===id);if(!account)throw new Error('GOOGLE_PHOTOS_ACCOUNT_NOT_FOUND');return account;}
  private async requireAccount(id: string, capability: GooglePhotosCapability) { const account = await this.requireAnyAccount(id); if (!account.capabilities.includes(capability)) throw new Error(`GOOGLE_PHOTOS_CAPABILITY_REQUIRED:${capability}`); return account; }
"""
          if old not in s: raise SystemExit('google photos savedAccounts block not found')
          s=s.replace(old,new)
          s=s.replace("await fs.writeFile(path.join(this.options.accountsDir, accountFilename(account.id)), JSON.stringify({ ...account, tokens: client.credentials, updatedAt: new Date().toISOString() }, null, 2)", "await fs.writeFile(path.join(this.options.accountsDir, accountFilename(this.options.workspaceId, account.id)), JSON.stringify({ ...account, workspaceId:this.options.workspaceId, tokens: client.credentials, updatedAt: new Date().toISOString() }, null, 2)")
          gp.write_text(s)

          env=Path('desktop/.env.example')
          es=env.read_text()
          if 'PHOTOX_WEB_PUBLIC_BASE_URL' not in es:
            es += "\n# Web edge (same UI as Desktop)\nPHOTOX_WEB_ENABLED=false\nPHOTOX_WEB_HOST=127.0.0.1\nPHOTOX_WEB_PORT=43118\nPHOTOX_WEB_PUBLIC_BASE_URL=https://photos.example.com\nPHOTOX_WEB_ALLOWED_ORIGINS=https://photos.example.com\nPHOTOX_WEB_RATE_LIMIT=300\n"
          env.write_text(es)
          PY
      - run: npm install
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
      - name: Commit verified changes and remove temporary workflow
        run: |
          rm .github/workflows/v4-run8-hardening.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "harden web refresh cookies and scope Google Photos accounts"
          git push origin HEAD:v4
