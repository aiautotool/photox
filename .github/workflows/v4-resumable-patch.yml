name: V4 resumable migration patch
on:
  push:
    branches: [v4]
    paths: ['.github/workflows/v4-resumable-patch.yml']
permissions:
  contents: write
jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v4
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply resumable checkpoint implementation
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace(path, old, new):
              p=Path(path); s=p.read_text()
              if old not in s:
                  raise SystemExit(f'expected snippet missing: {path}: {old[:90]!r}')
              p.write_text(s.replace(old,new,1))

          # Google Drive protocol helpers.
          p=Path('packages/google-drive/src/index.ts'); s=p.read_text()
          s=s.replace("uploadType=resumable&fields=id,name,size,createdTime", "uploadType=resumable&fields=id,name,mimeType,size,createdTime,webViewLink")
          marker="\nexport function driveDownloadUrl(fileId: string): string {"
          helpers=r'''

export type DriveResumableUploadStatus =
  | { state: 'active'; committedBytes: number }
  | { state: 'completed'; file: DriveFile }
  | { state: 'expired' };

function committedBytesFromRange(value: string | null): number {
  if (!value) return 0;
  const match = /^bytes=0-(\d+)$/.exec(value.trim());
  return match ? Number(match[1]) + 1 : 0;
}

async function completedDriveFile(response: Response): Promise<DriveFile> {
  const file = await response.json() as DriveFile;
  if (!file?.id) throw new Error('GOOGLE_DRIVE_DESTINATION_ID_MISSING');
  return file;
}

/** Query a Google Drive resumable session without re-uploading bytes. */
export async function queryResumableUploadSession(
  sessionUri: string,
  totalBytes: number,
  request: typeof fetch = fetch,
): Promise<DriveResumableUploadStatus> {
  const response = await request(sessionUri, {
    method: 'PUT',
    headers: { 'Content-Length': '0', 'Content-Range': `bytes */${Math.max(0, Math.floor(totalBytes))}` },
  });
  if (response.status === 308) return { state: 'active', committedBytes: committedBytesFromRange(response.headers.get('range')) };
  if (response.status === 404 || response.status === 410) return { state: 'expired' };
  if (response.status === 200 || response.status === 201) return { state: 'completed', file: await completedDriveFile(response) };
  throw new Error(`Drive resumable status ${response.status}: ${await response.text()}`);
}

/** Upload one aligned chunk to an existing resumable session. */
export async function uploadResumableChunk(
  sessionUri: string,
  input: { bytes: Uint8Array; startByte: number; totalBytes: number; mimeType?: string; signal?: AbortSignal },
  request: typeof fetch = fetch,
): Promise<DriveResumableUploadStatus> {
  const startByte = Math.max(0, Math.floor(input.startByte));
  const totalBytes = Math.max(0, Math.floor(input.totalBytes));
  if (!input.bytes.byteLength) throw new Error('GOOGLE_DRIVE_RESUMABLE_EMPTY_CHUNK');
  const endByte = startByte + input.bytes.byteLength - 1;
  if (endByte >= totalBytes) {
    if (endByte !== totalBytes - 1) throw new Error(`GOOGLE_DRIVE_RESUMABLE_RANGE_INVALID:${startByte}:${endByte}:${totalBytes}`);
  }
  const response = await request(sessionUri, {
    method: 'PUT',
    headers: {
      'Content-Type': input.mimeType || 'application/octet-stream',
      'Content-Length': String(input.bytes.byteLength),
      'Content-Range': `bytes ${startByte}-${endByte}/${totalBytes}`,
    },
    body: input.bytes,
    signal: input.signal,
  });
  if (response.status === 308) return { state: 'active', committedBytes: committedBytesFromRange(response.headers.get('range')) };
  if (response.status === 404 || response.status === 410) return { state: 'expired' };
  if (response.status === 200 || response.status === 201) return { state: 'completed', file: await completedDriveFile(response) };
  throw new Error(`Drive resumable chunk ${response.status}: ${await response.text()}`);
}
'''
          if marker not in s: raise SystemExit('google-drive marker missing')
          p.write_text(s.replace(marker,helpers+marker,1))

          # Migration checkpoint contract and runner persistence.
          p=Path('packages/google-photos/src/migration.ts'); s=p.read_text()
          s=s.replace("export type MigrationItemState = 'queued' | 'downloading' | 'uploading' | 'verifying' | 'completed' | 'failed' | 'cancelled';\n", "export type MigrationItemState = 'queued' | 'downloading' | 'uploading' | 'verifying' | 'completed' | 'failed' | 'cancelled';\n\nexport interface MigrationTransferCheckpoint {\n  kind: 'google_drive_resumable_v1';\n  accountId: string;\n  sessionUri: string;\n  nextByte: number;\n  totalBytes: number;\n  targetId?: string;\n  updatedAt: string;\n}\n")
          s=s.replace("  updateItem(itemId: string, patch: Partial<Omit<GooglePhotosMigrationItem, 'id' | 'jobId' | 'sourceMediaId' | 'createdAt'>>): Promise<GooglePhotosMigrationItem | null>;\n", "  updateItem(itemId: string, patch: Partial<Omit<GooglePhotosMigrationItem, 'id' | 'jobId' | 'sourceMediaId' | 'createdAt'>>): Promise<GooglePhotosMigrationItem | null>;\n  getTransferCheckpoint(itemId: string): Promise<MigrationTransferCheckpoint | null>;\n  setTransferCheckpoint(itemId: string, checkpoint: MigrationTransferCheckpoint | null): Promise<void>;\n")
          s=s.replace("    onBytes?: (transferredBytes: number) => void;\n", "    onBytes?: (transferredBytes: number) => void;\n    checkpoint?: MigrationTransferCheckpoint;\n    onCheckpoint?: (checkpoint: MigrationTransferCheckpoint | null) => Promise<void>;\n",1)
          old="""        const result = await this.adapter.transfer({
          job,
          item: latest,
          source,
          signal,
          onBytes: bytes => { void this.ledger.updateItem(item.id, { transferredBytes: Math.max(0, Math.floor(bytes)), updatedAt: this.now().toISOString() }); },
        });
"""
          new="""        const checkpoint = await this.ledger.getTransferCheckpoint(item.id) ?? undefined;
        const result = await this.adapter.transfer({
          job,
          item: latest,
          source,
          signal,
          checkpoint,
          onBytes: bytes => { void this.ledger.updateItem(item.id, { transferredBytes: Math.max(0, Math.floor(bytes)), updatedAt: this.now().toISOString() }); },
          onCheckpoint: async nextCheckpoint => { await this.ledger.setTransferCheckpoint(item.id, nextCheckpoint); },
        });
"""
          if old not in s: raise SystemExit('runner transfer snippet missing')
          s=s.replace(old,new,1)
          old="""        await this.ledger.updateItem(item.id, { state: 'completed', targetId: result.targetId, targetUrl: result.targetUrl, error: undefined, updatedAt: this.now().toISOString() });
"""
          new="""        await this.ledger.updateItem(item.id, { state: 'completed', targetId: result.targetId, targetUrl: result.targetUrl, error: undefined, updatedAt: this.now().toISOString() });
        await this.ledger.setTransferCheckpoint(item.id, null);
"""
          if old not in s: raise SystemExit('runner completion snippet missing')
          p.write_text(s.replace(old,new,1))

          # SQLite durable checkpoint column + accessors (not exposed through item snapshots).
          p=Path('packages/persistence-sqlite/src/migration.ts'); s=p.read_text()
          s=s.replace("  MigrationTarget,\n", "  MigrationTarget,\n  MigrationTransferCheckpoint,\n")
          s=s.replace("        target_url TEXT,\n        error TEXT,", "        target_url TEXT,\n        checkpoint_json TEXT,\n        error TEXT,")
          old="""    `);
  }

  async createJob"""
          new="""    `);
    const itemColumns = new Set((this.store.db.prepare('PRAGMA table_info(photox_migration_items)').all() as Array<{ name: string }>).map(row => row.name));
    if (!itemColumns.has('checkpoint_json')) this.store.db.exec('ALTER TABLE photox_migration_items ADD COLUMN checkpoint_json TEXT');
  }

  async createJob"""
          if old not in s: raise SystemExit('sqlite constructor marker missing')
          s=s.replace(old,new,1)
          marker="\n  private mapJob(row: Record<string, unknown>): GooglePhotosMigrationJob {"
          methods="""

  async getTransferCheckpoint(itemId: string): Promise<MigrationTransferCheckpoint | null> {
    const row = this.store.db.prepare('SELECT checkpoint_json FROM photox_migration_items WHERE id=?').get(itemId) as { checkpoint_json?: string | null } | undefined;
    return row?.checkpoint_json ? JSON.parse(row.checkpoint_json) as MigrationTransferCheckpoint : null;
  }

  async setTransferCheckpoint(itemId: string, checkpoint: MigrationTransferCheckpoint | null): Promise<void> {
    this.store.db.prepare('UPDATE photox_migration_items SET checkpoint_json=?,updated_at=COALESCE(updated_at,?) WHERE id=?').run(
      checkpoint ? JSON.stringify(checkpoint) : null, new Date().toISOString(), itemId,
    );
  }
"""
          if marker not in s: raise SystemExit('sqlite method marker missing')
          p.write_text(s.replace(marker,methods+marker,1))

          # Desktop migration service passes durable state to Drive and preserves progress on retry.
          p=Path('desktop/electron/googlePhotosMigration.ts'); s=p.read_text()
          s=s.replace("  type MigrationTarget,\n", "  type MigrationTarget,\n  type MigrationTransferCheckpoint,\n")
          old="export type DriveMigrationDestination = (input: { accountId: string; source: PickedMediaItem; response: Response; signal?: AbortSignal; onBytes?: (bytes: number) => void }) => Promise<{ targetId?: string; targetUrl?: string }>;"
          new="export type DriveMigrationDestination = (input: { accountId: string; source: PickedMediaItem; response: Response; signal?: AbortSignal; onBytes?: (bytes: number) => void; checkpoint?: MigrationTransferCheckpoint; onCheckpoint?: (checkpoint: MigrationTransferCheckpoint | null) => Promise<void> }) => Promise<{ targetId?: string; targetUrl?: string }>;"
          if old not in s: raise SystemExit('drive destination type missing')
          s=s.replace(old,new,1)
          old="""    for (const item of (await this.options.ledger.listItems(jobId)).filter(item => item.state === 'failed')) await this.options.ledger.updateItem(item.id, { state: 'queued', error: undefined, transferredBytes: 0, updatedAt });
"""
          new="""    for (const item of (await this.options.ledger.listItems(jobId)).filter(item => item.state === 'failed')) {
      const checkpoint = await this.options.ledger.getTransferCheckpoint(item.id);
      await this.options.ledger.updateItem(item.id, { state: 'queued', error: undefined, transferredBytes: checkpoint?.nextByte ?? 0, updatedAt });
    }
"""
          if old not in s: raise SystemExit('retry snippet missing')
          s=s.replace(old,new,1)
          old="""      transfer: async ({ job: currentJob, source, signal, onBytes }) => {
        const response = await downloadPickedMedia(source); const contentLength = Number(response.headers.get('content-length') || 0);
        if (currentJob.target === 'google_drive') return this.options.uploadToDrive({ accountId: currentJob.targetAccountId, source, response, signal, onBytes });
"""
          new="""      transfer: async ({ job: currentJob, source, signal, onBytes, checkpoint, onCheckpoint }) => {
        const response = await downloadPickedMedia(source); const contentLength = Number(response.headers.get('content-length') || 0);
        if (currentJob.target === 'google_drive') return this.options.uploadToDrive({ accountId: currentJob.targetAccountId, source, response, signal, onBytes, checkpoint, onCheckpoint });
"""
          if old not in s: raise SystemExit('service transfer snippet missing')
          p.write_text(s.replace(old,new,1))

          # Desktop Drive uploader: 8 MiB chunks, session status recovery, checkpoint before/after chunks.
          p=Path('desktop/electron/main.ts'); s=p.read_text()
          s=s.replace("import { DRIVE_SCOPE, createResumableUploadSession, ensurePhotoSyncFolder, getDriveFile, getStorageQuota, listPhotoSyncFiles } from '@photosync/google-drive';", "import { DRIVE_SCOPE, createResumableUploadSession, ensurePhotoSyncFolder, getDriveFile, getStorageQuota, listPhotoSyncFiles, queryResumableUploadSession, uploadResumableChunk } from '@photosync/google-drive';")
          old="""async function uploadMigrationItemToDrive(input:{accountId:string;source:any;response:Response;signal?:AbortSignal;onBytes?:(bytes:number)=>void}){
  const account=(await runtimeDriveAccounts()).find(item=>item.id===input.accountId);if(!account)throw new Error('GOOGLE_DRIVE_DESTINATION_NOT_FOUND');
  const token=await account.client.getAccessToken();if(!token.token)throw new Error('GOOGLE_DRIVE_ACCESS_TOKEN_MISSING');
  const bytes=Buffer.from(await input.response.arrayBuffer());if(input.signal?.aborted)throw new Error('MIGRATION_ABORTED');
  const mimeType=input.source.mediaFile?.mimeType||'application/octet-stream';const filename=safeFilename(input.source.mediaFile?.filename||`google-photo-${Date.now()}`);
  const session=await createResumableUploadSession(token.token,{name:filename,mimeType,sizeBytes:bytes.byteLength,folderId:account.folderId,appProperties:{photoxMigration:'true',sourceMediaId:String(input.source.id)}});
  const uploaded=await net.fetch(session,{method:'PUT',headers:{'content-type':mimeType,'content-length':String(bytes.byteLength)},body:bytes});
  if(!uploaded.ok)throw new Error(`Drive migration upload ${uploaded.status}: ${await uploaded.text()}`);input.onBytes?.(bytes.byteLength);
  const result=await uploaded.json() as {id?:string};if(!result.id)throw new Error('GOOGLE_DRIVE_DESTINATION_ID_MISSING');
  const verified=await getDriveFile(token.token,result.id);if(Number(verified.size||0)!==bytes.byteLength)throw new Error(`GOOGLE_DRIVE_SIZE_MISMATCH:${verified.size||0}:${bytes.byteLength}`);
  return {targetId:verified.id,targetUrl:verified.webViewLink};
}
"""
          new=r'''async function uploadMigrationItemToDrive(input:{accountId:string;source:any;response:Response;signal?:AbortSignal;onBytes?:(bytes:number)=>void;checkpoint?:{kind:'google_drive_resumable_v1';accountId:string;sessionUri:string;nextByte:number;totalBytes:number;targetId?:string;updatedAt:string};onCheckpoint?:(checkpoint:any|null)=>Promise<void>}){
  const account=(await runtimeDriveAccounts()).find(item=>item.id===input.accountId);if(!account)throw new Error('GOOGLE_DRIVE_DESTINATION_NOT_FOUND');
  const token=await account.client.getAccessToken();if(!token.token)throw new Error('GOOGLE_DRIVE_ACCESS_TOKEN_MISSING');
  const mimeType=input.source.mediaFile?.mimeType||'application/octet-stream';const filename=safeFilename(input.source.mediaFile?.filename||`google-photo-${Date.now()}`);
  const headerBytes=Number(input.response.headers.get('content-length')||0);
  let checkpoint=input.checkpoint?.kind==='google_drive_resumable_v1'&&input.checkpoint.accountId===input.accountId?input.checkpoint:undefined;
  let totalBytes=Number.isFinite(headerBytes)&&headerBytes>0?Math.floor(headerBytes):checkpoint?.totalBytes||0;
  if(!totalBytes){
    const bytes=Buffer.from(await input.response.arrayBuffer());if(input.signal?.aborted)throw new Error('MIGRATION_ABORTED');totalBytes=bytes.byteLength;
    const session=await createResumableUploadSession(token.token,{name:filename,mimeType,sizeBytes:totalBytes,folderId:account.folderId,appProperties:{photoxMigration:'true',sourceMediaId:String(input.source.id)}});
    const uploaded=await uploadResumableChunk(session,{bytes,startByte:0,totalBytes,mimeType,signal:input.signal});
    if(uploaded.state!=='completed')throw new Error('GOOGLE_DRIVE_RESUMABLE_INCOMPLETE');input.onBytes?.(totalBytes);
    const verified=await getDriveFile(token.token,uploaded.file.id);if(Number(verified.size||0)!==totalBytes)throw new Error(`GOOGLE_DRIVE_SIZE_MISMATCH:${verified.size||0}:${totalBytes}`);
    return {targetId:verified.id,targetUrl:verified.webViewLink};
  }
  if(checkpoint&&checkpoint.totalBytes!==totalBytes)checkpoint=undefined;
  if(checkpoint?.targetId){
    const verified=await getDriveFile(token.token,checkpoint.targetId);if(Number(verified.size||0)!==totalBytes)throw new Error(`GOOGLE_DRIVE_SIZE_MISMATCH:${verified.size||0}:${totalBytes}`);input.onBytes?.(totalBytes);return {targetId:verified.id,targetUrl:verified.webViewLink};
  }
  let sessionUri=checkpoint?.sessionUri||'';let nextByte=checkpoint?.nextByte||0;
  if(sessionUri){
    const status=await queryResumableUploadSession(sessionUri,totalBytes);
    if(status.state==='completed'){
      await input.onCheckpoint?.({kind:'google_drive_resumable_v1',accountId:input.accountId,sessionUri,nextByte:totalBytes,totalBytes,targetId:status.file.id,updatedAt:new Date().toISOString()});
      const verified=await getDriveFile(token.token,status.file.id);if(Number(verified.size||0)!==totalBytes)throw new Error(`GOOGLE_DRIVE_SIZE_MISMATCH:${verified.size||0}:${totalBytes}`);input.onBytes?.(totalBytes);return {targetId:verified.id,targetUrl:verified.webViewLink};
    }
    if(status.state==='expired'){sessionUri='';nextByte=0;await input.onCheckpoint?.(null)}else nextByte=status.committedBytes;
  }
  if(!sessionUri){
    sessionUri=await createResumableUploadSession(token.token,{name:filename,mimeType,sizeBytes:totalBytes,folderId:account.folderId,appProperties:{photoxMigration:'true',sourceMediaId:String(input.source.id)}});nextByte=0;
  }
  const persistCheckpoint=async(targetId?:string)=>input.onCheckpoint?.({kind:'google_drive_resumable_v1',accountId:input.accountId,sessionUri,nextByte,totalBytes,targetId,updatedAt:new Date().toISOString()});
  await persistCheckpoint();input.onBytes?.(nextByte);
  const body=input.response.body;if(!body)throw new Error('GOOGLE_DRIVE_SOURCE_STREAM_MISSING');
  const reader=body.getReader();const chunkSize=8*1024*1024;let skip=nextByte;let pending=Buffer.alloc(0);let completedId:string|undefined;
  const send=async(bytes:Buffer)=>{
    if(input.signal?.aborted)throw new Error('MIGRATION_ABORTED');const start=nextByte;
    const result=await uploadResumableChunk(sessionUri,{bytes,startByte:start,totalBytes,mimeType,signal:input.signal});
    if(result.state==='expired')throw new Error('GOOGLE_DRIVE_RESUMABLE_SESSION_EXPIRED');
    if(result.state==='completed'){nextByte=totalBytes;completedId=result.file.id;await persistCheckpoint(completedId);input.onBytes?.(nextByte);return}
    const expected=start+bytes.byteLength;if(result.committedBytes!==expected)throw new Error(`GOOGLE_DRIVE_RESUMABLE_OFFSET_MISMATCH:${expected}:${result.committedBytes}`);
    nextByte=result.committedBytes;await persistCheckpoint();input.onBytes?.(nextByte);
  };
  while(true){const {value,done}=await reader.read();if(done)break;if(!value?.byteLength)continue;let chunk=Buffer.from(value);if(skip){const consumed=Math.min(skip,chunk.byteLength);skip-=consumed;chunk=chunk.subarray(consumed);if(!chunk.byteLength)continue}pending=pending.length?Buffer.concat([pending,chunk]):chunk;while(pending.length>=chunkSize&&!completedId){const part=pending.subarray(0,chunkSize);pending=Buffer.from(pending.subarray(chunkSize));await send(part)}}
  if(skip)throw new Error(`GOOGLE_DRIVE_SOURCE_SHORTER_THAN_CHECKPOINT:${skip}`);if(pending.length&&!completedId)await send(pending);
  if(!completedId){const status=await queryResumableUploadSession(sessionUri,totalBytes);if(status.state!=='completed')throw new Error('GOOGLE_DRIVE_RESUMABLE_INCOMPLETE');completedId=status.file.id;nextByte=totalBytes;await persistCheckpoint(completedId)}
  const verified=await getDriveFile(token.token,completedId);if(Number(verified.size||0)!==totalBytes)throw new Error(`GOOGLE_DRIVE_SIZE_MISMATCH:${verified.size||0}:${totalBytes}`);
  return {targetId:verified.id,targetUrl:verified.webViewLink};
}
'''
          if old not in s: raise SystemExit('main drive migration function missing')
          p.write_text(s.replace(old,new,1))

          # Runner checkpoint behavior test.
          p=Path('packages/google-photos/src/index.test.ts'); s=p.read_text()
          test=r'''

  it('persists a transfer checkpoint after failure and clears it only after verified completion', async () => {
    const ledger = new MemoryLedger();
    const job = makeJob('job-checkpoint');
    await ledger.createJob(job);
    const [item] = migrationItemsFromPicker(job.id, [source('a')]);
    await ledger.putItems([item]);
    let first = true;
    const checkpoint = { kind: 'google_drive_resumable_v1' as const, accountId: 'target-account', sessionUri: 'https://upload/session', nextByte: 8, totalBytes: 16, updatedAt: '2026-09-03T00:00:00.000Z' };
    const runner = new GooglePhotosMigrationRunner(ledger, {
      async transfer({ checkpoint: existing, onCheckpoint }) {
        if (first) { first = false; expect(existing).toBeUndefined(); await onCheckpoint?.(checkpoint); throw new Error('network dropped'); }
        expect(existing).toEqual(checkpoint); return { targetId: 'drive-final' };
      },
      async verify() {},
    });
    const sources = new Map([['a', source('a')]]);
    const failed = await runner.run(job.id, sources, { shouldPause: () => false, shouldCancel: () => false });
    expect(failed.state).toBe('completed_with_errors');
    expect(await ledger.getTransferCheckpoint(item.id)).toEqual(checkpoint);
    const completed = await runner.run(job.id, sources, { shouldPause: () => false, shouldCancel: () => false });
    expect(completed.state).toBe('completed');
    expect(await ledger.getTransferCheckpoint(item.id)).toBeNull();
  });
'''
          marker='\n});\n\nfunction source'
          if marker not in s: raise SystemExit('google photos test marker missing')
          s=s.replace(marker,test+marker,1)
          s=s.replace("  items = new Map<string, GooglePhotosMigrationItem>();\n", "  items = new Map<string, GooglePhotosMigrationItem>();\n  checkpoints = new Map<string, any>();\n")
          s=s.replace("  async updateItem(itemId: string, patch: Partial<GooglePhotosMigrationItem>) { const item = this.items.get(itemId); if (!item) return null; const next = { ...item, ...patch }; this.items.set(itemId, next); return { ...next }; }\n", "  async updateItem(itemId: string, patch: Partial<GooglePhotosMigrationItem>) { const item = this.items.get(itemId); if (!item) return null; const next = { ...item, ...patch }; this.items.set(itemId, next); return { ...next }; }\n  async getTransferCheckpoint(itemId: string) { return this.checkpoints.get(itemId) ?? null; }\n  async setTransferCheckpoint(itemId: string, checkpoint: any | null) { if (checkpoint) this.checkpoints.set(itemId, checkpoint); else this.checkpoints.delete(itemId); }\n")
          p.write_text(s)

          # SQLite process-restart persistence test.
          Path('packages/persistence-sqlite/src/migration.test.ts').write_text(r'''import { mkdtemp, rm } from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import { afterEach, describe, expect, it } from 'vitest';
import { SqliteGooglePhotosMigrationLedger, SqlitePhotoXStore } from './index';

const cleanup: string[] = [];
afterEach(async () => { while (cleanup.length) await rm(cleanup.pop()!, { recursive: true, force: true }); });

describe('migration resumable checkpoint persistence', () => {
  it('survives store close/reopen without exposing the session URI in migration item snapshots', async () => {
    const dir = await mkdtemp(path.join(os.tmpdir(), 'photox-migration-')); cleanup.push(dir); const dbPath = path.join(dir, 'migration.sqlite');
    let store = new SqlitePhotoXStore({ path: dbPath }); let ledger = new SqliteGooglePhotosMigrationLedger(store);
    const now='2026-09-03T00:00:00.000Z';
    await ledger.createJob({ id:'job',workspaceId:'ws',sourceAccountId:'src',target:'google_drive',targetAccountId:'drive',state:'queued',totalItems:1,completedItems:0,failedItems:0,transferredBytes:0,createdAt:now,updatedAt:now });
    await ledger.putItems([{id:'item',jobId:'job',sourceMediaId:'source',filename:'video.mp4',mimeType:'video/mp4',sizeBytes:16,state:'failed',attempts:1,transferredBytes:8,createdAt:now,updatedAt:now}]);
    const checkpoint={kind:'google_drive_resumable_v1' as const,accountId:'drive',sessionUri:'https://upload.example/secret-session',nextByte:8,totalBytes:16,updatedAt:now};
    await ledger.setTransferCheckpoint('item',checkpoint); store.close();
    store=new SqlitePhotoXStore({path:dbPath});ledger=new SqliteGooglePhotosMigrationLedger(store);
    expect(await ledger.getTransferCheckpoint('item')).toEqual(checkpoint);
    expect(JSON.stringify(await ledger.listItems('job'))).not.toContain('secret-session');
    await ledger.setTransferCheckpoint('item',null);expect(await ledger.getTransferCheckpoint('item')).toBeNull();store.close();
  });
});
''')

          # Google Drive helper tests and root wiring.
          Path('packages/google-drive/src/index.test.ts').write_text(r'''import { afterEach, describe, expect, it, vi } from 'vitest';
import { queryResumableUploadSession, uploadResumableChunk } from './index';
afterEach(() => vi.restoreAllMocks());
describe('Drive resumable protocol', () => {
  it('recovers the committed byte offset from a session status query', async () => {
    const request=vi.fn(async()=>new Response(null,{status:308,headers:{Range:'bytes=0-524287'}}));
    await expect(queryResumableUploadSession('https://upload/session',1048576,request as any)).resolves.toEqual({state:'active',committedBytes:524288});
    expect(new Headers(request.mock.calls[0][1]?.headers).get('content-range')).toBe('bytes */1048576');
  });
  it('sends an exact chunk range and returns completed Drive metadata', async () => {
    const request=vi.fn(async(_url:string,init?:RequestInit)=>{expect(new Headers(init?.headers).get('content-range')).toBe('bytes 524288-524291/524292');return new Response(JSON.stringify({id:'drive-file',name:'v.mp4',mimeType:'video/mp4',size:'524292'}),{status:200,headers:{'content-type':'application/json'}})});
    await expect(uploadResumableChunk('https://upload/session',{bytes:new Uint8Array([1,2,3,4]),startByte:524288,totalBytes:524292,mimeType:'video/mp4'},request as any)).resolves.toEqual(expect.objectContaining({state:'completed',file:expect.objectContaining({id:'drive-file'})}));
  });
});
''')
          p=Path('packages/google-drive/package.json'); import json; d=json.loads(p.read_text()); d['scripts']['test']='vitest run'; d.setdefault('devDependencies',{})['vitest']='^3.2.4'; p.write_text(json.dumps(d,indent=2)+'\n')
          p=Path('package.json'); d=json.loads(p.read_text()); d['scripts']['test']=d['scripts']['test'].replace('npm --workspace @photosync/core test &&', 'npm --workspace @photosync/core test && npm --workspace @photosync/google-drive test &&'); p.write_text(json.dumps(d,indent=2)+'\n')
          PY
      - run: npm install
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
      - name: Commit validated batch to v4 and remove patch workflow
        shell: bash
        run: |
          git config user.name 'PhotoX V4 Automation'
          git config user.email 'actions@users.noreply.github.com'
          git rm .github/workflows/v4-resumable-patch.yml
          git add packages/google-drive packages/google-photos packages/persistence-sqlite desktop/electron package.json package-lock.json
          git commit -m 'resume Google Drive migrations across restarts'
          git push origin HEAD:v4
